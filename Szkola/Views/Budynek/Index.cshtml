@model IEnumerable<Szkola.Models.Budynek>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ulica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nr_budynku)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kod_pocztowy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Miejscowosc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ulica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nr_budynku)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kod_pocztowy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Miejscowosc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BudynekID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BudynekID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BudynekID })
        </td>
    </tr>
}

</table>
